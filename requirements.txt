# Discord (py-cord – wird benötigt für Slash Commands / ApplicationContext)
py-cord>=2.4.0

# KI-Parser (Claude Haiku)
anthropic>=0.25.0

# Google Sheets
gspread>=6.0.0
google-auth>=2.0.0

# HTTP (Grabber)
requests>=2.31.0

# Fuzzy Matching
rapidfuzz>=3.0.0

# System-Stats (/system Befehl)
psutil>=5.9.0

# Umgebungsvariablen
python-dotenv>=1.0.0

# Lokalisierte Ländernamen (CLDR-Daten)
Babel>=2.8.0

# Voice (optional, unterdrückt discord voice-warning / behebt Voice-Fehlermeldungen)
PyNaCl>=1.5.0
davey

# Ranking-Bild (iNat-Treppchen, lokal gerendert)
matplotlib>=3.7.0

# Feedback-Board (cogs/board.py): Webserver + Templates.
# aiohttp ist ohnehin py-cord-Abhängigkeit; hier explizit + Jinja2 (Templates, Autoescape).
aiohttp>=3.9.0
Jinja2>=3.1.0
